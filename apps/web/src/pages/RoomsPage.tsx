function RoomsPage() {
  return (
    <section className="page-card">
      <h2 className="page-title">Rooms</h2>
      <p className="page-subtitle">Mock rooms directory with CTA to join hottest room.</p>
      <div className="route-grid">
        <div className="route-chip">Room list (mock data)</div>
        <div className="route-chip">Filters & sorting (mock)</div>
        <div className="route-chip">CTA: Join hottest room</div>
      </div>
    </section>
  );
}

export default RoomsPage;
