function LoginPage() {
  return (
    <section className="page-card">
      <h2 className="page-title">Login</h2>
      <p className="page-subtitle">Mock login form placeholder for Phase 0.</p>
      <div className="route-grid">
        <div className="route-chip">Email/password mock</div>
        <div className="route-chip">Social buttons (mock)</div>
      </div>
    </section>
  );
}

export default LoginPage;
